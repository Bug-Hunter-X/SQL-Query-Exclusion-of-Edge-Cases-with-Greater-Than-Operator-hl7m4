```sql
SELECT * FROM employees WHERE department = 'Sales' AND salary >= 100000;
```
The solution uses the `>=` (greater than or equal to) operator to include employees with salaries equal to 100000, ensuring all relevant employees are returned.